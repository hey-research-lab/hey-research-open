/**
 * Reserved (2026-09-26) for the shapes of a project snapshot, coverage and explain, so that work
 * adds its types here without editing `index.ts`, which already re-exports
 * this file with `export type *`. Empty until then.
 */
export {};
